# Capability

## Overview

A capability is an unforgeable token of authority that simultaneously establishes a reference to a [Claim](claim.md) and grants a specific set of access rights (permissions) to that claim.

## Details

A capability is non-repudiable and can only be obtained through legitimate means (e.g. delegation).
