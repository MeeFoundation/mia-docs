## 1. pdn-store: an entry affects only its own key

- [x] 1.1 The ranger `Store` trait per D1 — `entry_get` in place of `prefixes_of` and `remove_prefix_filtered`, `put` and `would_insert` comparing against the entry of the same author at the same key, an empty one included; the fs store's `entry_get` reading the live store with empty entries and refusing a foreign namespace, its parent iterator gone; the session store passing `entry_get` through unfiltered; the in-memory test store and the reconciliation model `insert_if_larger` per key; verify with `just test -p pdn-store` and `just check-store`
- [x] 1.2 The delete at one key per D2 — `Replica::delete`, the actor's delete action, `Doc::del` taking a key and `DelRequest` carrying `key`, their docs saying what the count answers; verify that `doc_delete` in `tests/sync.rs` passes and that `delete_prefix` appears nowhere in the workspace
- [x] 1.3 The store's tests of prefix deletion rewritten for one key — `test_delete_touches_only_its_key_*`, `test_replica_sync_delete_*` asserting the deleted key absent on both replicas, `test_replica_delete_edge_cases_*`, `an_identifier_naming_another_namespace_is_refused`; verify with `just test-store`
- [x] 1.4 The subset-reconciliation scenario "A delete converges whichever side opens the session": `an_older_entry_does_not_replace_a_delete_at_its_key` and `replicas_converge_on_a_delete_whichever_side_initiates` in `sync.rs`; verify that both pass, and that both fail with `entry_get` excluding empty entries
- [x] 1.5 The subset-reconciliation scenario "A delete inside a session lands in two parts": a store test beside `a_retraction_inside_a_session_is_served_for_the_rest_of_it` in `net/codec.rs`, deleting after the session's view is taken; verify that it passes, and that it fails with the session serving the live store in place of its frozen view
- [x] 1.6 The store's `CLAUDE.md` records the divergence from upstream's prefix semantics per D5; verify by reading it against D1 and D2

## 2. data-layer: the stores over the per-key rule

- [x] 2.1 `prune_retractions` per D3, listing this device's markers for the issuer and deleting each at its key, and the doc of `prune_aged_retractions` without the over-drop it describes; verify with `markers_record_list_and_prune_by_issuer`
- [x] 2.2 The data-store scenario "A write at a shorter path leaves the longer ones standing": a scenario test on two devices of one identity writing `contact/email`, `contacts/emergency`, then `contact`, listing and reading all three on both; verify that it passes, and that it fails with the store's prefix removal restored in `put`
- [x] 2.3 The directory scenario "A marker at a path leaves the markers at longer paths" in `retraction_markers.rs`, the sibling's reading included; verify that it passes, and that it fails with the store's prefix removal restored in `put`
- [x] 2.4 Two `test-util` hooks on `SyncNode` per D4 — `sync_namespace_as_for_test`, one session of a replica named by its namespace toward a contact, and `leave_swarm_for_test`, the replica out of its gossip swarm with its reconciliation left running; verify through their use in 2.5
- [x] 2.5 The connection-metadata-store scenario "A withdrawal holds against a device that still holds the record" in `connection_metadata.rs`: the audience's data session served under the grant, the audience's replica out of its swarm, the withdrawal on the publishing device, the publishing device opening the session of its own store toward the audience's device through the hooks of 2.4, later sessions both ways, then both devices reading no grant and the same data session refused as its paired denial; verify that it passes, and that it fails with `entry_get` excluding empty entries

## 3. Specs, archive and the sibling change

- [x] 3.1 The archive rewritten as present state where it names the removed trait methods or prefix deletion — the frozen-session-snapshots design and tasks, the disk-persistence design and delta, the identity-scoped-replicas deltas; verify that a grep of `openspec/` for `prefixes_of`, `remove_prefix_filtered`, `delete_prefix` and "prefix delet" finds only this change's own text
- [x] 3.2 Every other active change that describes the store's prefix deletion or the removed methods rewritten as present state through the update workflow; verify that the grep of 3.1 over `openspec/changes/` finds only this change's own text, and that `openspec validate` passes for each change touched
- [x] 3.3 `openspec validate --all --strict` passes, every delta's `#### Scenario:` heading checked by eye

## 4. Stress and the pipeline's checks

- [x] 4.1 The stress pass of `specs/code-practices/flaky-tests.md` over the scenarios the change reaches, 300 iterations — `just stress --stress-count 300 --no-fail-fast -E '(kind(test) & (binary(connection_metadata) | binary(retraction_markers) | binary(sync_two_devices) | binary(pending_devices) | test(/withdraw|retract|tombstone|delete|disconnect|revok/))) | (package(pdn-store) & kind(lib) & test(/delete|retraction/))'`: every connection-metadata, retraction-marker, two-device and pending-device scenario, every scenario of a withdrawal, retraction, tombstone, delete, disconnect or revocation, and the store's delete tests; verify that no iteration fails, any failure diagnosed as a defect of this change
- [x] 4.2 `just precommit-check` — audit, lint, build, the workspace's tests, the store's other feature sets and the container suite; verify that it passes
